<html>
<body>
<div>
    <!--кнопка выхода. видна после авторизации-->
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        <input type="submit" value="Sing out"/>
    </form>
</div>
<div>
    <!--форма будет отправляться на PostMapping-->
    <form method="post">
        <input type="text" name="text" placeholder="Введите сообщение"/>
        <input type="text" name="tag" placeholder="Tэг">
        <!--это токен от мусташа с доп функционалом против хакеров. добавляется во все поля запросов-->
        <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        <button type="submit">Добавить</button>
    </form>
</div>
<div>Список сообщений</div>
<!--добавляем разметку и новый маппинг filter-->
<form method="post" action="filter">
    <input type="text" name="filter">
    <!--это токен от мусташа с доп функционалом против хакеров. добавляется во все поля запросов-->
    <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
    <button type="submit">Найти</button>
</form>
<!--вывод всех сообщений-->
{{#messages}}
    <div>
        <b>{{id}}</b>
        <span>{{text}}</span>
        <i>{{tag}}</i>
        <strong>{{authorName}}</strong>
    </div>
{{/messages}}
</body>
</html>
